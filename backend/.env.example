# GitHub OAuth 配置
# 支持本地和生产环境两套 OAuth App
# 后端会自动尝试两套凭证，任意一套成功即可

# 本地开发 OAuth App (http://localhost:5173)
GITHUB_CLIENT_ID_LOCAL=your_local_client_id
GITHUB_CLIENT_SECRET_LOCAL=your_local_client_secret

# 生产环境 OAuth App (https://learngraph.online)
GITHUB_CLIENT_ID_PROD=your_prod_client_id
GITHUB_CLIENT_SECRET_PROD=your_prod_client_secret

# 管理员邮箱（只有这个邮箱的 GitHub 账号可以登录）
ADMIN_EMAIL=brycew6m@gmail.com
